# --- AI SDKs ---
anthropic>=0.40.0
google-genai>=2.0.0
# Perplexity and xAI use OpenAI-compatible HTTP; we use httpx directly.
httpx>=0.27.0

# --- Calendar / mail (pick the ones you'll use) ---
google-auth>=2.30.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.130.0
msgraph-sdk>=1.5.0          # optional, only if using Microsoft Graph

# --- Config / logging ---
python-dotenv>=1.0.0
pyyaml>=6.0
structlog>=24.1.0

# --- Windows automation (Phase 4+) ---
pywinauto>=0.6.8 ; sys_platform == "win32"
uiautomation>=2.0 ; sys_platform == "win32"
pyautogui>=0.9.54

# --- Dev ---
pytest>=8.0
ruff>=0.5.0
